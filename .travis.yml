sudo: false

language: node_js

addons:
    apt:
        sources:
            -   google-chrome
        packages:
            -   google-chrome-stable

cache:
    directories:
        -   ./node_modules

install:
    -   npm install

# script:
#     -   npm test

jobs:
    include:
    -   name: ng build
        script: npm run build
    -   name: test
        script: npm test
        

    